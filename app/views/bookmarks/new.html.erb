<h1>New Bookmark</h1>

<%= simple_form_for([@list, @bookmark]) do |f| %>
  <%= f.input :movie_id, collection: @movies, prompt: "Choose a movie" %>
  <%= f.input :comment %>

  <%= f.button :submit %>
<% end %>

<%= link_to "Back to list", list_path(@list) %>
